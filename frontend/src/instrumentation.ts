// Simple instrumentation file to prevent import errors
// Sentry configuration can be added later if needed

export async function register() {
  // No instrumentation for now
  console.log('Instrumentation loaded');
}